{
  "name": "tickr.id",
  "private": true,
  "workspaces": [
    "backend",
    "web"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w backend",
    "dev:web": "npm run dev -w web",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w web",
    "lint": "npm run lint -w web"
  }
}
